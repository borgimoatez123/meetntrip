<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Booking Confirmed</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #4CAF50; color: white; padding: 10px; text-align: center; }
        .content { padding: 20px; }
        .footer { margin-top: 20px; font-size: 0.8em; text-align: center; color: #777; }
        .booking-details { margin-top: 20px; }
        .detail-row { margin-bottom: 10px; }
        .detail-label { font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Booking Confirmed</h1>
        </div>
        
        <div class="content">
            <p>Dear {{ user.nom }},</p>
            <p>We are pleased to inform you that your booking has been confirmed.</p>
            
            <div class="booking-details">
                <h2>Booking Details</h2>
                
                <div class="detail-row">
                    <span class="detail-label">Event:</span> {{ booking.nameEvement }}
                </div>
                <div class="detail-row">
                    <span class="detail-label">Booking ID:</span> {{ booking.bookingId }}
                </div>
                <div class="detail-row">
                    <span class="detail-label">Dates:</span> 
                    {{ booking.startEvement|date('F j, Y') }} to {{ booking.endEvement|date('F j, Y') }}
                </div>
                <div class="detail-row">
                    <span class="detail-label">Number of Invites:</span> {{ booking.numberofInvites }}
                </div>
                
                {% if booking.specialRequests %}
                <div class="detail-row">
                    <span class="detail-label">Special Requests:</span> {{ booking.specialRequests }}
                </div>
                {% endif %}
                
                <h3>Services Included</h3>
                
                {% if booking.flight %}
                <div class="detail-row">
                    <span class="detail-label">Flight:</span> {{ booking.airlines }}
                    (Departure: {{ booking.departureTime|date('H:i') }}, 
                    Return: {{ booking.backTime|date('H:i') }})
                </div>
                {% endif %}
                
                {% if booking.hotel %}
                <div class="detail-row">
                    <span class="detail-label">Hotel:</span> {{ booking.hotelName }} ({{ booking.hotelLocation }})
                </div>
                {% endif %}
                
                {% if booking.transport %}
                <div class="detail-row">
                    <span class="detail-label">Transport:</span> {{ booking.transportType }}
                </div>
                {% endif %}
                
                {% if booking.conferenceLocation %}
                <div class="detail-row">
                    <span class="detail-label">Conference:</span> {{ booking.conferenceName }}
                </div>
                {% endif %}
                
                <div class="detail-row">
                    <span class="detail-label">Total Price:</span> ${{ booking.priceTotal }}
                </div>
            </div>
            
            <p>Thank you for choosing our services. If you have any questions, please don't hesitate to contact us.</p>
        </div>
        
        <div class="footer">
            <p>This is an automated message. Please do not reply directly to this email.</p>
        </div>
    </div>
</body>
</html>